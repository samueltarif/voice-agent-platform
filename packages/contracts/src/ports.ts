export interface TelephonyPort {
  readonly providerName: string;
}

export interface RealtimeAIPort {
  readonly providerName: string;
}

export interface StoragePort {
  readonly providerName: string;
}
